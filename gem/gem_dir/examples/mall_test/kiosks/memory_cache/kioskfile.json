{
  "name": "memory_cache",
  "version": "0.2.0",
  "language": "java",
  "description": "This kiosk is a pipeline to cache results from another kiosk for a set period of time.",

  "settings": {
    "Timeout": {
      "description": "Number of seconds before the cached item expires",
      "default": "86400"
    }
  },

  "dependencies": {
    "target": {
      "resource": "resource",
      "kiosk": ["*", "*"],
      "description": "An in-memory cache will store the content service index for TIMEOUT seconds"
    }
  },

  "namespace": "sample"
}
